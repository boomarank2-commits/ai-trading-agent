{
  "$schema": "https://schema.freqtrade.io/schema.json",
  "_comment": "Public-data/dry-run overlay for CCXT 4.5.73. Binance market discovery stays public (apiKey null). FreqUI is enabled only on localhost for the paper-trading test UI. STARTBOT injects unique local API credentials through environment variables.",
  "exchange": {
    "ccxt_config": {
      "apiKey": null
    },
    "ccxt_async_config": {
      "apiKey": null
    }
  },
  "api_server": {
    "enabled": true,
    "listen_ip_address": "127.0.0.1",
    "listen_port": 8080,
    "verbosity": "error",
    "enable_openapi": false,
    "jwt_secret_key": "LOCAL_ENV_REQUIRED_JWT_32_CHARS_MINIMUM",
    "ws_token": "LOCAL_ENV_REQUIRED_WS_32_CHARS_MINIMUM",
    "CORS_origins": [],
    "username": "testbot",
    "password": "LOCAL_ENV_REQUIRED"
  }
}
