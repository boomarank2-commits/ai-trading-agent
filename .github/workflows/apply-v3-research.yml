name: Apply V3 research patch

on:
  push:
    branches:
      - agent/strategy-v3-confirmed-breakout
    paths:
      - .github/workflows/apply-v3-research.yml
      - research/strategy-v3-confirmed-breakout.patch

permissions:
  contents: write

jobs:
  apply-v3:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/strategy-v3-confirmed-breakout
          fetch-depth: 0
      - name: Apply strategy patch
        shell: bash
        run: |
          git apply --check research/strategy-v3-confirmed-breakout.patch
          git apply research/strategy-v3-confirmed-breakout.patch
      - name: Commit strategy only
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm research/strategy-v3-confirmed-breakout.patch
          git rm .github/workflows/apply-v3-research.yml
          git add runtime/user_data/strategies/CompressionBreakout250.py
          git commit -m "Add confirmed breakout strategy V3"
          git push origin HEAD:agent/strategy-v3-confirmed-breakout
